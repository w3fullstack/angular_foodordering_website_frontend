import { NbMenuItem } from '../../';
/**
 * Context menu component used as content within NbContextMenuDirective.
 *
 * @styles
 *
 * context-menu-fg
 * context-menu-active-fg
 * context-menu-active-bg
 * */
export declare class NbContextMenuComponent {
    items: NbMenuItem[];
    tag: string;
}
